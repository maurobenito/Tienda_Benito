@{
    ViewData["Title"] = "Reportes de Ventas";
}

<h2>Reportes de Ventas</h2>

<div class="row g-4">
    <!-- Entre fechas -->
    <div class="col-md-6">
        <form method="get" action="/Home/ReporteEntreFechas" class="card card-body shadow-sm">
            <h5>Ventas entre fechas</h5>
            <div class="row">
                <div class="col">
                    <label>Desde</label>
                    <input type="date" name="desde" class="form-control" required />
                </div>
                <div class="col">
                    <label>Hasta</label>
                    <input type="date" name="hasta" class="form-control" required />
                </div>
            </div>
            <button type="submit" class="btn btn-primary mt-3"><i class="bi bi-search"></i> Ver Reporte</button>
        </form>
    </div>

    <!-- Por producto -->
    <div class="col-md-6">
        <form method="get" action="/Home/ReportePorProducto" class="card card-body shadow-sm">
            <h5>Ventas por producto</h5>
            <div class="mb-2">
                <input type="text" name="nombreProducto" class="form-control" placeholder="Nombre del producto" />
            </div>
            <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i> Ver Reporte</button>
        </form>
    </div>

    <!-- Por cliente -->
    <div class="col-md-6">
        <form method="get" action="/Home/ReportePorCliente" class="card card-body shadow-sm">
            <h5>Ventas por cliente</h5>
            <div class="mb-2">
                <input type="text" name="nombreCliente" class="form-control" placeholder="Nombre o apellido del cliente" />
            </div>
            <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i> Ver Reporte</button>
        </form>
    </div>
</div>
