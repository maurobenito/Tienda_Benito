@{
    Layout = null;
    ViewData["Title"] = "Bienvenido";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>Tienda Benito</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <style>
        body {
            background-image: url('/uploads/fondo-login.jpg'); /* Imagen de fondo */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            height: 100vh;
        }

        .bienvenida-container {
            background-color: rgba(255, 255, 255, 0.15); /* Más transparente */
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(6px); /* mejora la lectura sobre fondo */
        }

        .titulo-principal {
            font-weight: bold;
            font-size: 2.5rem;
            color: #333;
        }

        .subtitulo {
            font-size: 1.2rem;
            color: #555;
        }

        .btn {
            font-size: 1.1rem;
            padding: 10px 25px;
        }
    </style>
</head>
<body>
    <div class="container h-100 d-flex align-items-center justify-content-center">
        <div class="col-md-7 text-center bienvenida-container">
            <h1 class="titulo-principal">🛒 Bienvenido a Tienda Benito</h1>
            <p class="subtitulo mt-3">Tu sistema de gestión de ventas al alcance de un clic.</p>
            <a href="/Usuario/Login" class="btn btn-success mt-4">Ingresar al sistema</a>
        </div>
    </div>
</body>
</html>
